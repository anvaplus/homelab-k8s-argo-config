type: postgresql
mode: standalone

version:
  postgresql: "16"

cluster:
  instances: 3
  storage:
    size: 1Gi
    storageClass: "longhorn-cnpg"
  initdb:
    database: keycloak
    owner: keycloak
    secret:
      name: keycloak-secret

backups:
  enabled: false
